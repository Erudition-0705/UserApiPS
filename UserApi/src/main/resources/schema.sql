CREATE TABLE users (
    id INT PRIMARY KEY,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    age INT,
    gender VARCHAR(10),
    role VARCHAR(50),
    ssn VARCHAR(50),
    email VARCHAR(100),
    phone VARCHAR(50),
    image VARCHAR(200)
);
